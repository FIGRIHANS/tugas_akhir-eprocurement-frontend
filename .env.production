# Frontend Production Environment Variables
# Configuration for codeverse.id subdomain setup

# Production URLs - Each service has its own subdomain
VITE_API_BASE_URL=https://account-api.codeverse.id
VITE_API_GENERAL_BASE_URL=https://api.codeverse.id
VITE_API_VENDOR_BASE_URL=https://vendor-api.codeverse.id
VITE_API_INVOICE_BASE_URL=https://invoice-api.codeverse.id

# Local Testing (uncomment for local development)
# VITE_API_BASE_URL=http://localhost/api/account
# VITE_API_GENERAL_BASE_URL=http://localhost/api/general
# VITE_API_VENDOR_BASE_URL=http://localhost/api/vendor
# VITE_API_INVOICE_BASE_URL=http://localhost/api/invoice

# IMPORTANT NOTES:
# 1. These URLs are built into the frontend during 'docker build'
# 2. DNS must be configured for ALL subdomains pointing to VPS IP: 103.123.45.67
#    - evox.codeverse.id → 103.123.45.67 (Frontend)
#    - account-api.codeverse.id → 103.123.45.67 (Account API)
#    - api.codeverse.id → 103.123.45.67 (General API)
#    - vendor-api.codeverse.id → 103.123.45.67 (Vendor API)
#   - invoice-api.codeverse.id → 103.123.45.67 (Invoice API)
# 3. NGINX will handle routing based on server_name (subdomain)
# 4. If you change domain, rebuild frontend:
#    docker-compose build frontend
#    docker-compose up -d frontend
